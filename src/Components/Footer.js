function Footer() {
  let height2 = (2 / 100) * window.screen.height;
  return (
    <div
      class="text-muted text-center"
      style={{ backgroundColor: "black", height: height2 }}
    >
      <p style={{ color: "White", fontSize: "90%" }}>Made by AAA</p>
    </div>
  );
}
export default Footer;
